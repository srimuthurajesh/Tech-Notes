import pandas as pd

PRIMARY OBJECT TYPE
	#DATAFRAME: rows & columns lik spreadsheet
	#SERIES: a single column 

DATA DIRECTLY GIVEN:
	data_set = {'Day':[1,2,3,4,5,6],"Visitots":[1000,700,6000,1000,400,350],'Sales':[20,25,23,15,10,34]}
	df = pd.DataFrame(data_set)
	print(df)			#print entire data as array
	print(df.shape)		#return (num_of_rows,num_of_columns)

DUMMY VARIABLES:
pd.get_dummies(df.columnName)		#will give a seperated dummy column dataframe	

DATA FROM CSV:
	data = pd.read_csv('sales.csv',index_col=0)		#index_col=>an imaginary column 0,1,2 will be removed
	data = pd.write_csv('sales.csv',index_col=0)
	

	1. SLICING
		print(df.head(2))	#gives only first two rows
		print(df.tail(2))	#gives only last two rows
	2. MERGING
		merge = pd.merge(df1,df2)	
	3.JOINING (it is Nan, if no row available while joining)
		joined=df1.join(df2)
	4.CHANGE INDEX & HEADERS
		df.set_index("Day",inplace=True)			#set Day column as the index
		df.rename(columns:{'oldName':'newName'})		#renaming header
	5.CONCATENATION
		concat = pd.concat([df1,df2])
	6.DROPING
		dropped = df.drop([col1,col2],axis='columns')	

DATA MUNGING:
	csv_file = pd.read_csv('FILE_PATH',index_col=0)
	csv_file.to_html('output_file_name.html')
	
MISSING DATA HANDLING:
	df.dropna()					#drop row, eventhough minimum one NaN NaT is present
	df.dropna(axis='columns')	#drop column, eventhough minimum one Nan Nat is present	
	df.dropna(how='all')		#drop row, only if all elements are NaN NaT
	df.dropna(thresh=2)			#drop row, minimum two NaN NaT present
	df.dropna(subset=['colum1','colum2'])	#drop if these columns have NaT NaN
	df.dropna(inplace=True)		#terila 
	
	
